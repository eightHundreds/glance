## 1. 设置与存储
- [ ] 1.1 在 `UserSettings` / `SettingsState` 中增加 `summaryPrompt` 字段，给出默认模版、长度校验与同步策略；确保 load/save API 兼容老版本。
- [ ] 1.2 为 prompt 写入提供 helper：支持自动回退默认文案、手动清空时存空字符串，并在状态中提供「是否为默认值」标记。

## 2. Options UI
- [ ] 2.1 在模型配置区域加入多行文本输入框，带提示文案与字符计数；输入结束后自动保存，并显示最近一次保存状态。
- [ ] 2.2 若 prompt 被清空或超过限制，提供即时错误提示并阻止保存；展示“恢复默认”操作以快速回退。

## 3. Summary 管道
- [ ] 3.1 内容脚本/后台在构造模型请求时注入 `summaryPrompt` 作为 system message，缺省时落回默认；必要时把 prompt 来源打入日志。

## 4. 验证
- [ ] 4.1 手动验证：不同浏览器中修改 prompt 后刷新 Options 页仍保持一致；清空 prompt 后恢复默认。
- [ ] 4.2 在 summary 触发后检查网络请求体包含 prompt，确保空值场景使用默认文案。
